---
interface TextProps {
  content: string;
  className?: string;
  'data-directus'?: string;
}

const { content, className = "", "data-directus": dataDirectus } = Astro.props;
---

<div
  class={`prose dark:prose-invert ${className}`}
  set:html={content}
  data-directus={dataDirectus}
/>
